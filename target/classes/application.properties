server.port=8082
spring.application.name=DCOMToSchool

# High Performance: Enable Java 21 Virtual Threads
spring.threads.virtual.enabled=true

# Database Configuration (Base - Prod)
spring.datasource.url=jdbc:postgresql://ep-withered-flower-a811hc2n-pooler.eastus2.azure.neon.tech:5432/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_4A1VduWMcaYT

# Connection Pool Settings (Spring Boot uses HikariCP by default)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000

# JPA / Hibernate Configuration
# 'validate' verifies the DB schema matches your entities on startup, but DOES NOT change the DB
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
